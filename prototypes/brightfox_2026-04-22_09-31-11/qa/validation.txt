Visual QA fallback note
Date: 2026-04-22

Attempted browser QA:
- Installed Playwright Chromium
- Browser launch failed on this host because `libatk-1.0.so.0` is missing
- `playwright install-deps chromium` could not complete without sudo access
- No alternate renderer was installed on the host

Fallback validation completed instead:
- `html, body` include `overflow-x: hidden`
- all images use `max-width: 100%` and `height: auto`
- main shell uses `width: min(100% - 2rem, var(--max))`
- content sections use opaque backgrounds
- layout switches from single-column to grid only at media queries
- local case-study image assets exist and resolve
- visible body text count is 496 words, within the required 350-550 range

Known limitation:
- No real browser screenshots were produced on this host
