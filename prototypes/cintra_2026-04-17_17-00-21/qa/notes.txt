Static QA fallback was used.

The prompt asked for browser screenshots at 390, 768, 1024, and 1440 px.
That was not possible here because the available Chromium binary could not start:

- missing shared library: libatk-1.0.so.0

Fallback checks completed instead:

- verified `html, body { overflow-x: hidden; }`
- verified all images use `max-width: 100%`
- verified the main shell uses a relative width token
- verified section cards use opaque backgrounds
- verified no fixed pixel widths were used on section containers
- verified no absolute-positioned layout blocks were used
- verified visible body copy stayed within the required text budget

Case study image fetch was attempted against Elinext source pages.
Those pages returned anti-bot challenge responses in this environment, so proof cards were rendered without images, as allowed by the prompt.
